<UserControl x:Class="pilot_test.ValueSliderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <DockPanel >
        <DockPanel.Resources>
        <Style TargetType="DockPanel" >
            <Setter Property="Margin" Value="4" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Slider" >
            <Setter Property="TickFrequency" Value="10" />
            <Setter Property="TickPlacement" Value="BottomRight" />
            <Setter Property="IsSnapToTickEnabled" Value="True" />
            <Setter Property="MinWidth" Value="128" />
        </Style>
            <Style TargetType="TextBox" >
                <Setter Property="DockPanel.Dock" Value="Right" />
                <Setter Property="TextAlignment" Value="Right" />
                <Setter Property="Width" Value="40" />
            </Style>
        </DockPanel.Resources>

            <Label Content="M1__Power" />
        <TextBox Text="{Binding ElementName=slPower,Path=Value,UpdateSourceTrigger=PropertyChanged}" />
        <Slider x:Name="slPower" Maximum="{Binding MotorMax}" Minimum="{Binding MotorMin}" Value="{Binding MotorPower}" />
        </DockPanel>
</UserControl>
